## ТЗ: Добавление метода авторизации «Звонок от пользователя» (Check Call)

## 1. Цель
Добавить новый метод авторизации, при котором пользователь сам совершает звонок на указанный номер. Метод должен быть интегрирован в текущий диспетчер и активироваться через конфиг.

## Дополнительно создать в базе две новые колонки для будущей логики:
- is_privacy_accepted = Column(Boolean, default=False)
- is_subscribed = Column(Boolean, default=False)

*База имеет данные пользователей, поэтому обновление должно быть безопасным.*

## 2. Архитектурные изменения

## 2.1. Конфигурация (на стороне сервера)
Расширить логику переменной AUTH_METHOD в .env:

* sms — (уже есть) отправка кода в СМС.
* call — (уже есть) входящий звонок-пароль.
* check_call — (новое) ожидание исходящего звонка от пользователя.

## 2.2. База данных (модель User)
Добавить новое необязательное поле (Optional):

* sms_check_id (String) — для хранения идентификатора проверки от SMS.ru.
* Существующие поля (sms_code, sms_code_expires_at) сохранить, они нужны для работы методов sms и call.

## 3. Логика нового метода (check_call)

## Этап 1: Инициализация (Запрос звонка)

   1. Сервер приложения делает запрос к SMS.ru на метод /callcheck/add.
   2. В ответ получаем check_id и call_phone (номер, на который должен позвонить юзер).
   3. Сохранение: check_id записывается в новое поле sms_check_id.
   4. Ответ фронтенду: Сервер передает фронтенду номер call_phone и инструкцию. Код (OTP) в этом методе не генерируется и не используется.

## Этап 2: Верификация через Webhook
Вместо периодических запросов от сервера к SMS.ru, система должна ожидать автоматическое уведомление от шлюза.

   1. Создание Callback-эндпоинта:
   * Реализовать публичный эндпоинт (например, POST /api/auth/webhook/sms-ru).
      * Эндпоинт должен принимать данные от SMS.ru. При поступлении звонка шлюз пришлет check_id и статус.
   2. Обработка уведомления:
   * Сервер находит пользователя по полученному check_id в поле sms_check_id.
      * Если статус подтверждает звонок (код 401), сервер помечает пользователя как is_verified = True и очищает sms_check_id.
   3. Взаимодействие с фронтендом:
   * Фронтенд по-прежнему может использовать упрощенный запрос к бэкенду или WebSocket, чтобы узнать, изменился ли статус is_verified в базе после прихода вебхука.
   
   
## 4. Требования к интерфейсу (Frontend)
При активном методе check_call:

   1. Скрыть поле ввода 4-х цифр.
   2. Отобразить кнопку-ссылку (tel:+7...) с текстом: «Совершите бесплатный звонок для подтверждения номера».
   3. После нажатия пользователем на кнопку вызова, интерфейс переходит в режим ожидания. Как только бэкенд обработает Webhook и обновит статус пользователя, фронтенд автоматически перенаправляет его в личный кабинет.

------------------------------
Важное примечание для разработчика:
В личном кабинете SMS.ru в разделе «Настройки Callback» необходимо будет прописать полный URL созданного эндпоинта, чтобы шлюз знал, куда отправлять данные.

------------------------------

Взаимодействие Webhook и Frontend:
Так как Webhook приходит на бэкенд «внезапно», фронтенд не узнает об этом мгновенно сам по себе. Чтобы реализовать автоматический переход в личный кабинет (пункт 4.3), разработчику нужно будет выбрать один из двух путей:
Простой (Short Polling): Фронтенд раз в 2–3 секунды дергает ваш внутренний API (например, /api/me или /api/auth/status), проверяя флаг is_verified. Как только бэкенд получил вебхук и сменил флаг в базе — фронтенд это увидит и редиректнет юзера.
Сложный (WebSocket/SSE): Держать открытое соединение (но для такой простой задачи это может быть избыточно).
Обычно выбирают первый вариант (опрос своего бэкенда), так как это проще всего в реализации и надежно работает.

------------------------------

# Авторизация звонком

**Последнее изменение:** 16.06.2025

## Заголовок

В качестве альтернативы отправке кодов и паролей по СМС, мы разработали систему авторизации пользователя по звонку с его телефона.

1. Вы запрашиваете у пользователя его номер телефона и передаете его нам по API.
2. В ответ мы возвращаем вам номер телефона (+7 (499) 555-5555), по которому должен позвонить ваш пользователь для авторизации. У пользователя есть 5 минут на совершение звонка.
3. Как только мы получаем звонок, мы его сбрасываем (чтобы звонок был бесплатным для пользователя) и отправляем вам по системе webhook HTTP оповещение, что авторизация пройдена (вы также можете запрашивать состояние самостоятельно через API, который представлен ниже).
4. Звонок бесплатен для звонящего (даже для тех, кто звонит из-за рубежа).

## Передача номера телефона пользователя

```
https://sms.ru/callcheck/add?api_id=********-b159-f104-6d80-c9a2222e3c3e&phone=795166776**&json=1
```

## Параметры

| Параметр | Обязательный | Описание |
|----------|--------------|----------|
| api_id | да | Ваш уникальный ключ (api_id). Этот способ авторизации - самый удобный и приведен в примере выше. Ваш api_id вы можете найти на главной странице личного кабинета |
| phone | да | Номер телефона пользователя, который необходимо авторизовать (с которого мы будем ожидать звонок) |
| json=1 | рекомендуется | Данный параметр вызывает ответ сервера в формате JSON, в котором предоставлено больше данных |

## Ответ сервера

При включенном параметре json=1:

```json
{
    "status": "OK",
    "status_code": 100,
    "check_id": "201737-542",
    "call_phone": "78005008275",
    "call_phone_pretty": "+7 (800) 500-8275",
    "call_phone_html": "&lt;a href=\"callto:78005008275\"&gt;+7 (800) 500-8275&lt;\a&gt;"
}
```

**Поля ответа:**
- `status` — Запрос выполнен успешно (нет ошибок в авторизации)
- `status_code` — Успешный код выполнения
- `check_id` — Идентификатор авторизации (необходимо сохранить в вашей базе для дальнейшей проверки состояния)
- `call_phone` — Номер телефона, на который должен позвонить пользователь со своего номера
- `call_phone_pretty` — Номер телефона, на который должен позвонить пользователь со своего номера, в более красивом виде
- `call_phone_html` — Активная ссылка для мобильных телефонов, при нажатии на которую начнется набор номера

Без json:

```
100 // Запрос выполнен
201737-542 // Идентификатор авторизации (необходимо сохранить в вашей базе для дальнейшей проверки ее состояния)
78005008275 // Номер телефона, на который должен позвонить ваш пользователь со своего номера для прохождения авторизации
+7 (800) 500-8275 // Номер телефона, на который должен позвонить ваш пользователь со своего номера, в более красивом виде
```

## Проверка статуса звонка

Мы настоятельно рекомендуем использовать бесплатную опцию webhook для более быстрого и удобного получения статусов проверок. Она позволяет вам не запрашивать многократно статус проверки - он будут автоматически отправляться на ваш сервер в реальном времени.

```
https://sms.ru/callcheck/status?api_id=********-b159-f104-6d80-c9a2222e3c3e&check_id=[идентификатор авторизации]&json=1
```

## Параметры

| Параметр | Обязательный | Описание |
|----------|--------------|----------|
| api_id | да | Ваш уникальный ключ (api_id). Этот способ авторизации - самый удобный и приведен в примере выше. Ваш api_id вы можете найти на главной странице личного кабинета: ********-b159-f104-6d80-c9a2222e3c3e |
| check_id | да | Идентификатор авторизации, полученный от нас в первом примере |
| json=1 | рекомендуется | Данный параметр вызывает ответ сервера в формате JSON, в котором предоставлено больше данных об отправленных сообщениях |

## Ответ сервера

При включенном параметре json=1:

```json
{
    "status": "OK",
    "status_code": 100,
    "check_status": "401",
    "check_status_text": "Авторизация по звонку: номер подтвержден"
}
```

**Поля ответа:**
- `status` — Запрос выполнен успешно (нет ошибок в формате запроса)
- `status_code` — Успешный код выполнения
- `check_status` — Статус авторизации
- `check_status_text` — Текстовое описание статуса авторизации

Без json:

```
100 // Запрос выполнен
401 // Статус проверки авторизации по телефону
```

## Пример на PHP (со включенным модулем curl)

Мы настоятельно рекомендуем использовать нашу библиотеку под PHP. Данный код указан только в качестве простого примера.

```php
$ch = curl_init("https://sms.ru/callcheck/add");
curl_setopt($ch, CURLOPT_RETURNTRANSFER, 1);
curl_setopt($ch, CURLOPT_TIMEOUT, 30);
curl_setopt($ch, CURLOPT_POSTFIELDS, array(
    "api_id" => "********-b159-f104-6d80-c9a2222e3c3e",
    "phone" => "795166776**", // Номер телефона, который надо проверить (с которого мы будем ожидать звонок)
    "json" => 1 // Для получения более развернутого ответа от сервера
));
$body = curl_exec($ch);
curl_close($ch);

$json = json_decode($body);
if ($json) { // Получен ответ от сервера
    print_r($json); // Для дебага
    if ($json->status == "OK") { // Запрос выполнился
        echo "Идентификатор проверки: $json->check_id";
        echo "Номер, на который пользователю надо позвонить: $json->call_number"; 
        echo "Номер, на который пользователю надо позвонить (в красивом виде): $json->call_number_pretty"; 

    } else { // Запрос не выполнился (возможно ошибка авторизации, параметрах, итд...)
        // Код ошибки: $json->status_code Текст ошибки: $json->status_text
    }
} else { // Запрос не выполнился Не удалось установить связь с сервером

}
```

## Пример на PHP без curl

Мы настоятельно рекомендуем использовать нашу библиотеку под PHP. Данный код указан только в качестве простого примера.

```php
$body = file_get_contents("https://sms.ru/callcheck/add?api_id=********-b159-f104-6d80-c9a2222e3c3e&phone=795166776**&json=1"); 

$json = json_decode($body);
print_r($json); // Для дебага
// Для разбора $json можно использовать кусок кода из предыдущего примера.
```

## Пример на curl

Если вы используете curl под Windows. То знак \перед знаками ? и & необходимо убрать.

```
curl https://sms.ru/callcheck/add\api_id=********-b159-f104-6d80-c9a2222e3c3e\phone=795166776**\json=1
```

## Коды ответа сервера

| Код | Описание |
|-----|----------|
| 100 | Запрос выполнен, номер добавлен в базу для подтверждения и мы ожидаем с него звонок |
| 202 | Номер телефона пользователя указан неверно |
| 400 | Номер пока не подтвержден (с указанного вами номера пока не было звонка на номер, который предоставили вам мы) |
| 401 | Номер подтвержден |
| 402 | Истекло время, отведенное для проверки, либо неправильно указан check_id |
